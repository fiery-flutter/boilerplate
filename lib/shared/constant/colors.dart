import 'package:flutter/material.dart';

import '../config.dart';

class AppColors {
  // Base Theme Color
  static const Color primaryColor = Color(Config.PRIMARY_COLOR);
  static const Color accentColor = Color(0xFFFFFFFF);
  static const Color disabledLightColor = Color(0xFFA6A6A6);

  // Component Color
  static const Color strongLightTextColor = Color(0xFF333333);
  static const Color regularLightTextColor = Color(0xFFA6A6A6);

  // Pallete Color

  // Spesifik Color (fixed color)
}
